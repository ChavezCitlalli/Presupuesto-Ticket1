const ParamsDTO = require("./params.dto");
const UserDTO = require('./user.dto');
const PresupuestoDTO = require('./presupuesto.dto');
const AuthDTO = require('./auth.dto');

module.exports = {
    PresupuestoDTO,
    UserDTO,
    ParamsDTO,
    AuthDTO
}